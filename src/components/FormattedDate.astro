---
import { formatDate } from '../lib/utils';

export interface Props {
	date: Date;
	class?: string
}

const { date, class: klass } = Astro.props;
---

<time datetime={date.toISOString()} class={klass}>
	{
		formatDate(date)
	}
</time>
